import { StyleSheet } from "react-native"

export const screenStyles = StyleSheet.create({
    screenContainer: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center"
    }
})